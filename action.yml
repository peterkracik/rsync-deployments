name: "RSync"
description: ""
author: "Peter Kracik"
inputs:
  command:
    description: "rsync command(s) to execute"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  color: "gray-dark"
  icon: "terminal"
